define( [
  './chart-util',
  './init-props',
  './colorbrew',
  './Chart'
], function ( utils, initProp, colorbrew ) {
  return {
    utils: utils,
    initProp: initProp,
		colorbrew: colorbrew
  };
} );
